import { Navigation } from '../Navigation/Navigation';
import { PhonebookHeader } from './Header.styled';

export const Header = () => {
    return (
        <PhonebookHeader>
            <Navigation />
        </PhonebookHeader>
    )
}